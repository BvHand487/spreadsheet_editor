#include "TableSerializer.h"
#include "Utility.h"


Table* TableSerializer::loadFromFile(const std::string &file_name, char delimiter)
{
    // ...
    return nullptr;
}

void TableSerializer::saveToFile(Table* table, const std::string &file_name, char delimiter)
{
    // ...
}
